import React, { memo } from 'react';

interface IProps {
  width: number;
}

const CardBack: React.FC<IProps> = ({ width }: IProps): JSX.Element => {
  return <></>;
};

export default memo(CardBack);
